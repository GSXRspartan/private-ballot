[package]
name = "tari-cc-private-ballot-anchor-transport"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[dependencies]
tari-cc-private-ballot-anchor = { path = "../anchor" }
tari-cc-private-ballot-protocol = { path = "../protocol" }

[dev-dependencies]
# Archive is used only by the offline-archive-independence integration test to
# construct a real OotleAnchorRecordV1 and ArchiveHashV1. It is not a runtime
# dependency of the transport contract.
tari-cc-private-ballot-archive = { path = "../archive" }

[lints]
workspace = true
