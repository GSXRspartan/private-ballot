{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "election-loader",
  "description": "Narrow capability for canonical election artifact selection and prepared-ballot export: permits only native file-open and file-save dialogs. Grants no filesystem write, no recursive directory scan, no shell execution, and no remote access. CSP remains strict (default-src 'self', no remote origins).",
  "windows": ["main"],
  "permissions": ["dialog:allow-open", "dialog:allow-save"]
}
