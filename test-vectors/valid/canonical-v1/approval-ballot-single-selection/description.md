# approval-ballot-single-selection

A public approval ballot selecting the stable `approve` option under an exactly-one-choice policy that also permits abstention.

Object family: `approval-ballot-payload-v1`.

The canonical CBOR bytes are authoritative. JSON and hexadecimal files are presentation artifacts.
