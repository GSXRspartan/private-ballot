# registry-snapshot-multi-member

A three-member voter-owned governance-key registry supplied in noncanonical input order and encoded in canonical key order.

Object family: `registry-snapshot-v1`.

The canonical CBOR bytes are authoritative. JSON and hexadecimal files are presentation artifacts.
