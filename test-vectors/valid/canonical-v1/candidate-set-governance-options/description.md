# candidate-set-governance-options

Three stable governance option identifiers with separate display names, supplied out of order and encoded by canonical identifier order.

Object family: `candidate-set-v1`.

The canonical CBOR bytes are authoritative. JSON and hexadecimal files are presentation artifacts.
