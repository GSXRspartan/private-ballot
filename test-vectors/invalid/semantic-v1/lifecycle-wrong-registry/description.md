# lifecycle-wrong-registry

A verified ballot uses a different registry commitment than the open lifecycle.

- Scenario selector: `lifecycle-wrong-registry`
- Expected rejection code: `LIFECYCLE_COMMITMENT_MISMATCH`
