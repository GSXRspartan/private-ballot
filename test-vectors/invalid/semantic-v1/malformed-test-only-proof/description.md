# malformed-test-only-proof

The reserved test-only verifier receives bytes that do not authenticate the statement.

- Scenario selector: `malformed-test-only-proof`
- Expected rejection code: `INVALID_DATA`
