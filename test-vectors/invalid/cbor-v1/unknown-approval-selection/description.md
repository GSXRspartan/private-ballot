# unknown-approval-selection

The approval payload selects an identifier absent from the frozen candidate set.

- Decoder target: `approval-payload-min1-max2`
- Expected rejection code: `UNKNOWN_CANDIDATE_ID`
- Encoded byte length: 3
