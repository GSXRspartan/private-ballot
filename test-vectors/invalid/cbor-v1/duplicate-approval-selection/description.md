# duplicate-approval-selection

The same candidate identifier appears twice in one approval payload.

- Decoder target: `approval-payload-min1-max2`
- Expected rejection code: `DUPLICATE_SELECTION`
- Encoded byte length: 5
