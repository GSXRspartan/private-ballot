# duplicate-registry-key

The same governance public-key byte string appears twice.

- Decoder target: `registry-snapshot`
- Expected rejection code: `DUPLICATE_GOVERNANCE_KEY`
- Encoded byte length: 5
