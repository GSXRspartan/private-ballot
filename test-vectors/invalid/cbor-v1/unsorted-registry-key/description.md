# unsorted-registry-key

Registry keys are encoded in descending rather than canonical order.

- Decoder target: `registry-snapshot`
- Expected rejection code: `NON_CANONICAL_CBOR`
- Encoded byte length: 5
